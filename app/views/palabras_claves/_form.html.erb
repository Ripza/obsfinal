<%= form_for(@palabras_clafe) do |f| %>
  <% if @palabras_clafe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@palabras_clafe.errors.count, "error") %> prohibited this palabras_clafe from being saved:</h2>

      <ul>
      <% @palabras_clafe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :palabra %><br />
    <%= f.text_area :palabra %>
  </div>
  <div class="field">
    <%= f.label :intension %><br />
    <%= f.check_box :intension %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
